# tools

Personal collection of interactive tools. Hosted via GitHub Pages.

## Adding a new tool

1. Drop the `.html` file into `/tools/`
2. Add a card for it in `index.html`
3. Push — live in ~30 seconds

## Local dev

No build step. Just open `index.html` in a browser.
